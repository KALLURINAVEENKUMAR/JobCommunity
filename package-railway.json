{
  "name": "job-community-app",
  "version": "1.0.0",
  "description": "Job Community App - Full Stack",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "install-backend": "cd backend && npm install",
    "postinstall": "npm run install-backend"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
